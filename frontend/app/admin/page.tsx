// TEMPORARY placeholder so the /admin route resolves for the layout (task F6).
// Replace with the real users-management page in task F7.
export default function AdminUsersPage() {
  return <div className="px-4 pt-6">משתמשים</div>;
}
